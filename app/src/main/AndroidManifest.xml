<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalLib">

        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:label="Đăng Ký Tài Khoản"
            android:parentActivityName=".auth.LoginActivity" />

        <activity
            android:name=".main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".search.SearchActivity"
            android:exported="false"
            android:label="Tìm kiếm"
            android:parentActivityName=".main.MainActivity" />

        <activity
            android:name=".search.BookDetailActivity"
            android:exported="false"
            android:label="Chi tiết sách"
            android:parentActivityName=".search.SearchActivity" />

        <activity
            android:name=".search.BookReaderActivity"
            android:exported="false"
            android:label="Đọc sách"
            android:parentActivityName=".search.BookDetailActivity" />

        <activity
            android:name=".payment.PaymentActivity"
            android:exported="true"
            android:parentActivityName=".search.BookDetailActivity"
            android:label="Merchant Demo"
            android:launchMode="singleTask"
            >

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="demozpdk"
                    android:host="app" />
            </intent-filter>

        </activity>

        <activity
            android:name=".admin.BookApprovalActivity"
            android:exported="false"
            android:label="Duyệt Sách Upload"
            android:parentActivityName=".main.MainActivity" />

        <activity
            android:name=".admin.BookApprovalDetailActivity"
            android:exported="false"
            android:label="Chi tiết sách cần duyệt"
            android:parentActivityName=".admin.BookApprovalActivity" />

    </application>

</manifest>